#!/bin/zsh

if (( $#@ > 0 )); then
  local cmd=$1; shift
  local REPLY
  local func=.znap.$cmd
  if [[ -v functions[$func] ]]; then
    $func $@
  else
    .znap.help "znap: '$cmd' is not a znap command"
    return 1
  fi
else
  .znap.help
fi
