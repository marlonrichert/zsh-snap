#!/bin/zsh
setopt localoptions NO_warncreateglobal
zmodload -F zsh/parameter p:funcstack
zmodload -F zsh/zselect b:zselect

private fd
exec {fd}< <(
  zselect -t 100
  print
)
zle -F "$fd" ..znap.auto-compile.callback
